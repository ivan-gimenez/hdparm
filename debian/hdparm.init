#!/bin/sh -e

case "$1" in
  start|restart|reload|force-reload)
  /etc/init.d/hdparm.dev "$1"
  ;;
  stop)
  exit 0
  ;;
  *)
  echo "Usage: $0 {stop|start|restart|reload|force-reload}" >&2
  exit 3
  ;;
esac
